import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yurt.app',
  appName: 'Altyncup',
  webDir: 'dist/yurt-customer/browser'
};

export default config;
